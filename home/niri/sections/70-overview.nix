# Секция 70-overview — сгенерирована из config.kdl, теперь редактируется как Nix.
# Возвращает KDL-строку, которая склеивается в home/niri/default.nix.
''
overview {
    backdrop-color "#777777"
}
''
